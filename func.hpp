#include <iostream>

using namespace std;

//all functions are referenced here

//add declarations here
double average(double num1, double num2);
long tenth_power(long num);
bool is_palindrome(string text);
inline
string nameCall(string name){
    return name;
}
//string nameCall(string name);